import styled from 'styled-components';

import { checkbox as theme } from '@thesis-ui/theme';

const Container = styled.div`
`;

const Addition = styled.div`
`;

const Checkbox = styled.checkbox`
`;

export { Container, Addition, Checkbox };
