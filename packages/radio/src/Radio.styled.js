import styled from 'styled-components';

import { radio as theme } from '@thesis-ui/theme';

const Container = styled.div`
`;

const Addition = styled.div`
`;

const Radio = styled.checkbox`
`;

export { Container, Addition, Radio };
