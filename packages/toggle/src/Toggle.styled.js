import styled from 'styled-components';

import { toggle as theme } from '@thesis-ui/theme';

const Container = styled.div`
`;

const Addition = styled.div`
`;

const Toggle = styled.checkbox`
`;

export { Container, Addition, Toggle };
