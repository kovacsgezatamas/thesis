import { color, spacing } from '@thesis-ui/tokens';

const loader = {
  size: '20px',
  smallSize: '16px',
  padding: spacing.xxs,
  border: `2px solid ${color.seaGreen}`,
  borderRadius: spacing.sm,
  background: color.white,
  progressBackground: color.seaGreenLight10,
};

export default loader;
