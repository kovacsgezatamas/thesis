import { spacing, color } from '@thesis-ui/tokens';

const spinner = {
  size: '48px',
  smallSize: '24px',
  color: color.seaGreen,
  fillColor: 'none',
  tickness: spacing.xs,
  smallTickness: spacing.xxs,
};

export default spinner;
