import styled from 'styled-components';

const BadgeHelper = styled.div`
  width: 80px;
  height: 80px;
  border: 2px solid #CCC;
  border-radius: 4px;
  position: relative;
`;

BadgeHelper.displayName = 'AnyContainer';

export default BadgeHelper;
